# Codex 入口

开始前先读：

1. `C:\Users\admin\AGENT_SHARED_CONTEXT.md`
2. `PROJECT_CONTEXT.md`
3. `STATUS.md`
4. `CLAUDE.md`

当前仓库是 `proposal-helper-mvp`。只在这个目录内操作，不要把旧的 `proposal-helper` 当成当前项目。

遵循用户要求：
- 中文优先
- 不动 8083 线上版本
- 只在本地副本里做视觉和功能调整

