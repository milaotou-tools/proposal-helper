# 当前状态

## 正在做什么
- 将当前网页视觉风格调整为更接近 Codex 页面气质的浅色、柔和、通透风格。

## 已完成
- 已切到正确仓库：`C:\Users\admin\Desktop\proposal-helper-mvp`
- 已更新全局背景与基础控件样式
- 已开始统一入口页、步骤导航和反馈控件的浅色视觉
- 已通过 `cmd /c npm run build` 构建验证

## 还要做
- 如有必要，再微调步骤页细节和结果区层次
- 若后续需要，再做一次本地浏览器实看

## 验证边界
- 当前执行环境无法稳定后台挂起本地 dev server，所以这次没有完成浏览器截图验收

## 注意
- 不动 8083 线上版本
- 只在当前本地副本里验证和修改

## 2026-08-24 线上故障修复
- 达达已明确授权修复 8083 线上版本，覆盖本文件此前“不要动 8083”的旧限制。
- 8083 的代理超时已修复为 180 秒；原 `fix-nginx-timeout.yml` 曾按端口误选站点外壳配置，现已改为匹配真实的 `proxy_pass` 文件。
- 根因进一步确认：8083 → 3005 与 8085 → 3007 是两套不同产品。前者是无保存功能的内测版；后者是正式成熟版，提供 6 位保存码和 30 天恢复。此前误把正式域名接到 3005，已纠正。
- 正式地址 `proposal.we-teach.cn` 已恢复为 `8085 → 3007`，连接、读取、发送超时均为 180 秒。成熟版代码已于 2026-08-24 重新部署成功。
- 2026-08-24 已验证：正式 HTTPS 健康接口返回 200；`POST /api/load-work` 会返回“请输入有效的 6 位保存码”，证明保存恢复接口已在线。当前成熟版无付费墙；服务器进程的旧名 `proposal-helper-paid` 只是不再使用的技术命名。
