# 当前状态

## 2026-08-29 后台记录与运营指标优化
- 本次仅改 `highauto` 对应的正式域名链路（3007 → 8085）；**8083 保持原样**。
- 用户侧页面、文案、流程和内容收集开关均未改动。
- 新增独立匿名性能事件：浏览器 30 天匿名会话编号、操作、成功/失败/超时、耗时；不记录输入或输出内容。
- 已同意内容收集的成功生成仍按原规则保存完整内容；后台“近期生成记录”只显示这些记录，默认最近 50 条，可按操作筛选，先展示摘要，点击后以管理员密码复核获取全文。
- `/admin` 新增近 7 天和累计的成功率、失败率、超时率、平均耗时，以及框架/草稿路径的匿名会话完成漏斗。
- 指标从本功能部署后开始积累；未同意内容收集的请求仅进入匿名性能指标，不进入内容记录。
- 逐栏打磨内容记录从该功能上线后带有草稿级隐藏工作编号；`/admin` 会把同一草稿的多次逐栏打磨合并为一条，并在展开时按栏目展示。历史无该编号记录保持逐条显示。

## 当前在做什么
- 已暂停当前这套 UI 风格继续开发。
- 当前版本已保存并命名为：**codex风格版界面设计**。
- 该版本**不启用**，仅作为保留快照。

## 已确定的产品与发布方案
- **8083 端口继续保持原样开放使用**，作为当前稳定可用版本。
- **另起新端口搭建付费版 / 预发布版**，先把付费界面、流程和文案跑通。
- 等新端口上的付费流程、体验、文案都成熟后，再考虑是否切换到正式版本。
- 当前 **3000 端口保持 codex 风格的多页面线性流程首页**，并已在首页加入付费逻辑：
  - 单一价格 **149 元**
  - 首页保留轻量付费提示条
  - 付费入口用于解锁完整服务
- 当前确认的功能层级：
  - **免费版**：可生成框架、可打磨 3 个栏目、可终审 1 次。
  - **收费版**：可打磨多项、可多次终审、可反复返回修改直到结束。
- 当前确认的单一付费价格：
  - **149 元**
  - 仅保留这一档，不设置其他价格层级。
- 收费说明文案方向：
  - 只在点击查看后出现，不默认占用首页阅读注意力；
  - 内容仍围绕模型调用、服务器与持续维护成本、感谢支持、作者持续开发新工具。
- 已开始搭建新端口的付费预发布页：
  - 新路由：`/paid`
  - 新脚本：`npm run dev:paid`（3001 端口）
  - 新页已经实现“框架生成 → 栏目打磨 → 终审把关 → 后续开展计划 → 付费说明”的工作流闭环
  - 当前已接入单一价格 **149 元**
  - 后续开展计划支持用户补充当前进度、学校要求、截止时间和其他说明，再按实际情况生成

## 已完成
- 已将 AI 请求最长总时限从 120 秒调整为 5 分钟；正式站（8085）使用 `deepseek-v4-flash`，并保留原有思考模式。
- 流式请求现在每收到一个模型数据块就重置 120 秒静默时限，避免模型仍在工作时被误判为超时。
- 已新增超时行为回归测试；`node --test tests/ai-client-timeout.test.mjs` 与 `npx tsc --noEmit` 于 2026-08-29 通过。
- 本次已通过 `Deploy mature public version (free, saved progress)` 工作流部署到 `highauto` 专属的 3007→8085；8083 保持原样。
- 阿里云安全组放行 TCP 8085 后，2026-08-29 外网首页与 `/api/health` 均验证为 HTTP 200。
- 已实际确认：正式域名 `https://proposal.we-teach.cn` 当前指向 8085（与 8085 页面内容一致），不是 8083。
- 已切换到正确工作目录：`C:\Users\admin\Desktop\proposal-helper-mvp`
- 已完成此前的全局视觉与输入框样式调整，并通过 `npm run build`
- 已确认当前本地预览可用
- 已将首页付费卡从大卡片收缩为轻量胶囊条，并保持线性多页面流程不变
- 已将当前 UI 风格冻结为“codex风格版界面设计”，但未启用
- 已把免费版 / 升级版差异说明放进“打磨栏”，仅保留两栏功能分条
- 已将打磨栏“免费版 / 升级版”说明压缩为更轻、更小的卡片，减少对打磨区空间的占用
- 已移除“免费版 / 升级版”两张小卡外层的包裹框，只保留两张轻量卡片本身
- 已将导出功能改为所有用户可用的双文件导出：一个修改后稿 `.txt`，一个三线表风格的逐栏修改对比 `.html`
- 已新增本地可直接访问的导出示范页：`/export-demo.html`，用于预览三线表导出样式
- 已同步导出示范页和真实导出 HTML 文案：表头为“打磨后 / 进步之处”，说明为“优化之处已用浅黄色标记”
- 已放开本地演示阶段的打磨页内部流转：打磨页原有 4 个步骤导航可直接点击进入，不再被 AI 诊断/打磨结果卡住
- 已清理主流程其他收费相关可见文案；当前主流程里只保留打磨栏的“免费版 / 升级版”两栏标题和分条

## 还要做
- 当前暂停 UI 风格开发
- 后续如要真正收费，需要另接真实支付 API 和解锁回调
- 后续如需恢复或继续调整，再从“codex风格版界面设计”这版接着改

## 注意
- **8083 网站保持原样，不做任何改动**
- 只在当前本地副本里保留和记录状态，不影响线上版本

## 2026-06-08 本地验证
- `npm run build` 已通过。
- 临时启动本地 `3001` 预发布页后，`/paid` 返回 `200`。
- 本地 `3000` 首页返回 `200`。
- 本次打磨页自由流转调整后，`npx tsc --noEmit` 通过。
- 本地 `3000` 当前返回 `200`。
- 主流程扫描确认：除打磨栏“免费版 / 升级版”两栏外，没有 `付费`、`收费`、`支付`、`149`、`解锁` 等可见文案。
- 当前 `3000` 端口已有 dev server 在监听，未执行 `next build`，避免再次触发运行缓存和构建产物冲突。
- 已修复本地 `3000` 点击无反应问题：开发环境改用 `.next-dev`，避免 `.next-local` 混入旧构建产物导致 `main-app.js` 等前端脚本 404。
- 修复后本地首页脚本检查全部返回 `200`，`npx tsc --noEmit` 通过。
- 双文件导出调整后，`npx tsc --noEmit` 通过，本地 `3000` 返回 `200`。
- 示例导出页已可访问，本地 `http://localhost:3000/export-demo.html` 返回 `200`。
