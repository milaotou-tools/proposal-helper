# 当前状态

## 正在做什么
- 将当前网页视觉风格调整为更接近 Codex 页面气质的浅色、柔和、通透风格。

## 已完成
- 已切到正确仓库：`C:\Users\admin\Desktop\proposal-helper-mvp`
- 已更新全局背景与基础控件样式
- 已开始统一入口页、步骤导航和反馈控件的浅色视觉
- 已通过 `cmd /c npm run build` 构建验证

## 还要做
- 如有必要，再微调步骤页细节和结果区层次
- 若后续需要，再做一次本地浏览器实看

## 验证边界
- 当前执行环境无法稳定后台挂起本地 dev server，所以这次没有完成浏览器截图验收

## 注意
- 不动 8083 线上版本
- 只在当前本地副本里验证和修改

## 2026-08-24 线上故障修复
- 达达已明确授权修复 8083 线上版本，覆盖本文件此前“不要动 8083”的旧限制。
- 8083 的代理超时已修复为 180 秒；原 `fix-nginx-timeout.yml` 曾按端口误选站点外壳配置，现已改为匹配真实的 `proxy_pass` 文件。
- 根因进一步确认：正式地址 `proposal.we-teach.cn` 的专用代理配置近期仍指向旧端口 `127.0.0.1:8085`，而当前课题助手的健康 PM2 进程监听 `3005`；这会在约 30 秒后产生 504。
- 已新增 `fix-public-domain-proxy.yml`，仅将 `proposal.we-teach.cn` 的专用代理改为 `127.0.0.1:3005`，并设置连接、读取、发送超时均为 180 秒。
- 2026-08-24 已验证：正式 HTTPS 健康接口返回 200；关闭数据保存的最小框架生成请求返回完整内容和 HTTP 200。
